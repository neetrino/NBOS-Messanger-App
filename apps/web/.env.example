# Prefer the monorepo root `.env` (see `/.env.example`) — include `NEXT_PUBLIC_API_URL` there.
# Use `apps/web/.env.local` only for web-only overrides (not committed).

NEXT_PUBLIC_API_URL=http://localhost:4000
