# All configuration lives in the repository root `.env` only.
# Copy `/.env.example` to `/.env` at the repo root. Expo injects `EXPO_PUBLIC_*` from there.
# Physical device: set EXPO_PUBLIC_API_URL (and NEXT_PUBLIC_API_URL) to your PC Wi‑Fi IP:4000.
